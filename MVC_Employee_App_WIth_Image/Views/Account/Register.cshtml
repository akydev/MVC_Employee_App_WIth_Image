@model MVC_Employee_App_WIth_Image.ViewModel.RegisterViewModel
@{
    ViewData["Title"] = "Register";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register — Employee Portal</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet" />

    <style>
        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #1e3a8a, #2563eb);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', sans-serif;
        }

        .auth-card {
            backdrop-filter: blur(18px);
            background: rgba(255, 255, 255, 0.92);
            border-radius: 18px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.25);
            padding: 2.5rem;
            width: 100%;
            max-width: 460px;
        }

        .auth-logo {
            width: 65px;
            height: 65px;
            border-radius: 50%;
            background: linear-gradient(135deg, #2563eb, #1e40af);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            color: #fff;
            font-size: 1.8rem;
        }

        .form-control {
            border-radius: 10px;
            padding: 0.6rem 0.75rem;
            border: 1px solid #e2e8f0;
            background: #f8fafc;
        }

            .form-control:focus {
                background: #fff;
                border-color: #2563eb;
                box-shadow: 0 0 0 0.15rem rgba(37,99,235,0.2);
            }

        .input-group-text {
            background: #f1f5f9;
            border-radius: 10px 0 0 10px;
            border: 1px solid #e2e8f0;
        }

        .btn-register {
            background: linear-gradient(135deg, #2563eb, #1e40af);
            border: none;
            color: white;
            font-weight: 600;
            padding: 0.7rem;
            border-radius: 10px;
            transition: 0.3s;
        }

            .btn-register:hover {
                background: linear-gradient(135deg, #1e40af, #1e3a8a);
                transform: translateY(-1px);
            }

        .password-strength {
            height: 5px;
            border-radius: 5px;
            margin-top: 6px;
            transition: 0.3s;
        }

        .link-text a {
            text-decoration: none;
            color: #1e40af;
            font-weight: 500;
        }

            .link-text a:hover {
                text-decoration: underline;
            }
    </style>
</head>
<body>

    <div class="auth-card">
        <div class="auth-logo">
            <i class="bi bi-person-plus"></i>
        </div>

        <h4 class="text-center fw-bold mb-1">Create Account</h4>
        <p class="text-center text-muted small mb-4">Join Employee Portal</p>

        @if (!ViewData.ModelState.IsValid)
        {
            <div asp-validation-summary="All" class="alert alert-danger py-2 small"></div>
        }

        <form asp-action="Register" method="post">
            @Html.AntiForgeryToken()

            <!-- Full Name -->
            <div class="mb-3">
                <label asp-for="FullName" class="form-label small fw-semibold"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-person"></i></span>
                    <input asp-for="FullName" class="form-control" placeholder="John Doe" />
                </div>
                <span asp-validation-for="FullName" class="text-danger small"></span>
            </div>

            <!-- Username -->
            <div class="mb-3">
                <label asp-for="UserName" class="form-label small fw-semibold"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-at"></i></span>
                    <input asp-for="UserName" class="form-control" placeholder="username" />
                </div>
                <span asp-validation-for="UserName" class="text-danger small"></span>
            </div>

            <!-- Email -->
            <div class="mb-3">
                <label asp-for="Email" class="form-label small fw-semibold"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                    <input asp-for="Email" class="form-control" placeholder="example@email.com" />
                </div>
                <span asp-validation-for="Email" class="text-danger small"></span>
            </div>

            <!-- Password -->
            <div class="mb-3">
                <label asp-for="Password" class="form-label small fw-semibold"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-lock"></i></span>
                    <input asp-for="Password" class="form-control" id="pwdInput"
                           oninput="checkStrength(this.value)" placeholder="Enter password" />
                    <span class="input-group-text" onclick="togglePwd('pwdInput','eye1')" style="cursor:pointer;">
                        <i class="bi bi-eye" id="eye1"></i>
                    </span>
                </div>
                <div id="strengthBar" class="password-strength bg-secondary"></div>
                <small id="strengthText"></small>
                <span asp-validation-for="Password" class="text-danger small"></span>
            </div>

            <!-- Confirm Password -->
            <div class="mb-4">
                <label asp-for="ConfirmPassword" class="form-label small fw-semibold"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-lock-fill"></i></span>
                    <input asp-for="ConfirmPassword" class="form-control" id="confirmPwd" />
                    <span class="input-group-text" onclick="togglePwd('confirmPwd','eye2')" style="cursor:pointer;">
                        <i class="bi bi-eye" id="eye2"></i>
                    </span>
                </div>
                <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
            </div>

            <button type="submit" class="btn btn-register w-100">
                <i class="bi bi-person-check me-2"></i>Create Account
            </button>
        </form>

        <hr class="my-3" />

        <p class="text-center small text-muted link-text">
            Already have an account?
            <a asp-controller="Account" asp-action="Login">Login</a>
        </p>
    </div>

    <script>
        function togglePwd(inputId, iconId) {
            const input = document.getElementById(inputId);
            const icon = document.getElementById(iconId);

            if (input.type === "password") {
                input.type = "text";
                icon.className = "bi bi-eye-slash";
            } else {
                input.type = "password";
                icon.className = "bi bi-eye";
            }
        }

        function checkStrength(val) {
            const bar = document.getElementById('strengthBar');
            const text = document.getElementById('strengthText');

            let strength = 0;
            if (val.length >= 6) strength++;
            if (/[A-Z]/.test(val)) strength++;
            if (/[0-9]/.test(val)) strength++;
            if (/[^A-Za-z0-9]/.test(val)) strength++;

            const colors = ["#ef4444", "#f59e0b", "#eab308", "#22c55e"];

            if (val.length === 0) {
                bar.style.width = "0%";
                text.textContent = "";
                return;
            }

            bar.style.width = (strength * 25) + "%";
            bar.style.background = colors[strength - 1] || "#ef4444";
            text.textContent = ["Weak", "Fair", "Good", "Strong"][strength - 1] || "";
        }
    </script>

</body>
</html>
@* @model MVC_Employee_App_WIth_Image.ViewModel.RegisterViewModel
@{
    ViewData["Title"] = "Register";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register — Employee Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet" />
    <style>
        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 50%, #2563eb 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', sans-serif;
            padding: 2rem 0;
        }

        .auth-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 25px 60px rgba(0,0,0,0.25);
            padding: 2.5rem;
            width: 100%;
            max-width: 440px;
        }

        .auth-logo {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            background: linear-gradient(135deg, #1e40af, #2563eb);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2rem;
            color: white;
        }

        .form-control:focus {
            border-color: #1e40af;
            box-shadow: 0 0 0 0.25rem rgba(30,64,175,0.15);
        }

        .btn-register {
            background: linear-gradient(135deg, #1e40af, #2563eb);
            border: none;
            color: white;
            font-weight: 600;
            padding: 0.75rem;
            border-radius: 10px;
            width: 100%;
            font-size: 1rem;
        }

            .btn-register:hover {
                background: linear-gradient(135deg, #1e3a8a, #1e40af);
                color: white;
            }

        .password-strength {
            height: 4px;
            border-radius: 2px;
            transition: all 0.3s;
            margin-top: 6px;
        }

        .input-group-text {
            background: #f8fafc;
            border-right: 0;
        }

        .form-control {
            border-left: 0;
        }
    </style>
</head>
<body>
    <div class="auth-card">
        <div class="auth-logo"><i class="bi bi-person-plus-fill"></i></div>
        <h3 class="text-center fw-bold mb-1" style="color:#1e40af;">Create Account</h3>
        <p class="text-center text-muted mb-4 small">Register to access Employee Portal</p>

        @if (!ViewData.ModelState.IsValid)
        {
            <div asp-validation-summary="All" class="alert alert-danger py-2 small rounded-3"></div>
        }

        <form asp-action="Register" method="post">
            @Html.AntiForgeryToken()
            <div class="mb-3">
                <label asp-for="FullName" class="form-label fw-semibold small"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-person-badge text-muted"></i></span>
                    <input asp-for="FullName" class="form-control" placeholder="Your full name" />
                </div>
                <span asp-validation-for="FullName" class="text-danger small"></span>
            </div>
            <div class="mb-3">
                <label asp-for="UserName" class="form-label fw-semibold small"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-at text-muted"></i></span>
                    <input asp-for="UserName" class="form-control" placeholder="Choose a username" />
                </div>
                <span asp-validation-for="UserName" class="text-danger small"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Password" class="form-label fw-semibold small"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-lock text-muted"></i></span>
                    <input asp-for="Password" class="form-control" id="pwdInput"
                           placeholder="Min. 6 characters" oninput="checkStrength(this.value)" />
                    <button type="button" class="input-group-text bg-white border-start-0"
                            onclick="togglePwd('pwdInput','eye1')" style="cursor:pointer;">
                        <i class="bi bi-eye" id="eye1"></i>
                    </button>
                </div>
                <div class="password-strength" id="strengthBar" style="background:#e2e8f0;"></div>
                <small id="strengthText" class="text-muted"></small>
                <span asp-validation-for="Password" class="text-danger small d-block"></span>
            </div>
            <div class="mb-4">
                <label asp-for="ConfirmPassword" class="form-label fw-semibold small"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-lock-fill text-muted"></i></span>
                    <input asp-for="ConfirmPassword" class="form-control" id="confirmPwd"
                           placeholder="Repeat your password" />
                    <button type="button" class="input-group-text bg-white border-start-0"
                            onclick="togglePwd('confirmPwd','eye2')" style="cursor:pointer;">
                        <i class="bi bi-eye" id="eye2"></i>
                    </button>
                </div>
                <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
            </div>
            <button type="submit" class="btn-register mb-3">
                <i class="bi bi-person-check me-2"></i>Create Account
            </button>
        </form>

        <hr class="my-3" />
        <p class="text-center text-muted small mb-0">
            Already have an account?
            <a asp-controller="Account" asp-action="Login" class="fw-semibold" style="color:#1e40af;">
                Sign in here
            </a>
        </p>
    </div>

    <script>
        function togglePwd(inputId, iconId) {
            const input = document.getElementById(inputId);
            const icon = document.getElementById(iconId);
            if (input.type === 'password') {
                input.type = 'text';
                icon.className = 'bi bi-eye-slash';
            } else {
                input.type = 'password';
                icon.className = 'bi bi-eye';
            }
        }

        function checkStrength(val) {
            const bar = document.getElementById('strengthBar');
            const text = document.getElementById('strengthText');
            let strength = 0;
            if (val.length >= 6) strength++;
            if (val.length >= 10) strength++;
            if (/[A-Z]/.test(val)) strength++;
            if (/[0-9]/.test(val)) strength++;
            if (/[^A-Za-z0-9]/.test(val)) strength++;

            const levels = [
                { color: '#ef4444', label: 'Very Weak', width: '20%' },
                { color: '#f97316', label: 'Weak', width: '40%' },
                { color: '#eab308', label: 'Fair', width: '60%' },
                { color: '#22c55e', label: 'Strong', width: '80%' },
                { color: '#16a34a', label: 'Very Strong', width: '100%' }
            ];
            const idx = Math.max(0, Math.min(strength - 1, 4));
            if (val.length > 0) {
                bar.style.background = levels[idx].color;
                bar.style.width = levels[idx].width;
                text.textContent = levels[idx].label;
                text.style.color = levels[idx].color;
            } else {
                bar.style.background = '#e2e8f0';
                bar.style.width = '100%';
                text.textContent = '';
            }
        }
    </script>
</body>
</html> *@
